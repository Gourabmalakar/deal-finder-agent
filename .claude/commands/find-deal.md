---
description: Find the cheapest verified price for a product across the top sites for its category
---

Run the `ecommerce-deal-finder` skill for this product: $ARGUMENTS

If that's a URL, open it to identify the exact product and variant. If it's
a description and the variant (color/storage/size) is ambiguous, ask before
searching. Follow the skill's steps and the report contract in
`docs/REPORT-FORMATS.md` exactly.
