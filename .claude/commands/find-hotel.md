---
description: Find the cheapest verified hotel rate for a place, dates, and guest count
---

Run the `hotel-deal-finder` skill for this search: $ARGUMENTS

If place, dates, or guest count are missing from that, ask before
searching — they're required inputs, not optional context. If any hotel
links were included, treat those as the target properties. Follow the
skill's steps and the report contract in `docs/REPORT-FORMATS.md` exactly.
